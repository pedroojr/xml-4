{
  "name": "xml-4",
  "private": true,
  "scripts": {
    "test:e2e": "TEST_BASE_URL=${TEST_BASE_URL:-http://localhost:4005} TEST_API_KEY=${TEST_API_KEY:-changeme} playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
